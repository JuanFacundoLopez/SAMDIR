function samdir_gui_graficarExcitacionTono(axes, excitacion)
%     cmap = hsv(length(excitacion));
%    for i=1:length(excitacion),
        plot(axes, excitacion(6).timeVector, excitacion(6).timeData,'c');
%        hold(axes, 'on')
%    end
end